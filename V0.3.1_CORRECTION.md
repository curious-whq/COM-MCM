# v0.3.1 语义修正

## 修正内容

v0.3 把 ready/valid/fire 描述成“状态语义之外的握手语义”，这个划分不准确。

修正后的统一语义是：

```text
Transformation:
  pre-state + 当周期组合 guard
  -> 发生事件
  -> post-state update
```

在 DCache 请求接口上：

```text
ReqValid ∧ ReqReady
  -> DCacheReqFire / RequestAccepted
```

- `valid`：发送条件的当周期观察；
- `ready`：接收条件的当周期观察；
- `fire`：转换 guard 成立后产生的接受事件；
- 后续状态变化仍由同一个 `Transformation` 的 `state_updates` 描述。

因此框架没有 `Handshake` 类，也没有 `handshakes:` 模型层。v0.3 示例末尾曾有一个 `handshakes:` 文档段，但解析器实际上没有消费它；v0.3.1 已删除该段，并让解析器拒绝所有未知顶层字段，避免以后再次静默忽略模型内容。

## 功能影响

这次是语义建模修正，不改变现有 BOOM witness：

```text
TLBMiss
-> RetryEnqueue
-> RetryIssue
-> TLBHit
-> ReqValid ∧ ReqReady
-> DCacheReqFire
```

变化在于最后一步现在被明确视为普通接口状态转换，而不是另一套独立语义。
